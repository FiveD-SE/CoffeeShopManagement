name: Run Tests

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Go to the CoffeeShopManagement directory
              run: cd CoffeeShopManagement
            - name: Run tests
              run: npm run test
